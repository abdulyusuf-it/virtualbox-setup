# virtualbox-setup
Step-by-step guide to installing and configuring Oracle VirtualBox for enterprise-style virtualization. This setup prepares the environment for creating and running Windows 11 virtual machines by configuring CPU, RAM, disk, EFI, and network settings (NAT + Internal).
